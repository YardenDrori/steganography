INSTALLATION:

install ffmpeg format for lossless file types on mac os the command is brew install ffmpeg on arch its sudo pacman -S ffmpeg (if i recall correctly) on windows idk lol figure it out

install sqlx
cargo install sqlx-cli --no-default-features --features postgres

ACTIVATION:
run the docker mysql server
    sudo systemctl start docker
run sqlx migrate run
