# Local Development Docker Compose
# This file includes individual service compose files
# Single source of truth: each service's own docker-compose.yml
# Edit services in their respective folders, not here!

include:
  - auth_service/docker-compose.yml
  - steganography_service/docker-compose.yml
  # - api_gateway/docker-compose.yml  # Uncomment when ready

# Note: Each service brings its own network
# Services can communicate across networks using service names
